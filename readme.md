# Flask API with SQLite and Test Coverage

## 💡 Description
A simple RESTful API using Flask and SQLite with full test coverage (unit, integration, API tests).

## 🔧 Stack
- Flask
- SQLAlchemy (SQLite backend)
- Pytest + Coverage

## 🚀 Run the App
```bash
pip install -r requirements.txt
python app.py
